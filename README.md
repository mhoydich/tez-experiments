# tez-experiments

A ladder of small, composable, CC0 Tezos templates. The wallet is a passport
that accumulates identity through activity.

01 `tez-onboard` · 02 `tez-stamps` · 03 `tez-nouns` · 04 `venice-drum-circle`

Agents and humans: start with **AGENTS.md** (instructions + backlog).
Each template has its own README and is independently copyable.
